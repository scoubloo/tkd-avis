node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# Secrets : jamais dans le dépôt. Le fichier réel vit dans INFRA/.secrets/
.env
.env.local
.env.production

# Données vivantes de PostgreSQL si quelqu'un lance la pile en local.
# Sans cette ligne, un `git add -A` après un `docker compose up` commiterait un
# cluster entier — adresses e-mail et empreintes comprises.
donnees/

# Sorties de test
test-results/
playwright-report/
blob-report/
coverage/
